\documentclass[text.tex]{subfiles}

\begin{document}


\section{8-fold rotational symmetry}
\subsection{Definition}
\begin{align*}
x^2 &=2x+1 &
\beta_8 &= 1 + \sqrt{2} \doteq 2.414 &
\beta_8' &= 1 - \sqrt{2} \doteq -0.414 \\
\end{align*}

\subsection{Formulas}
\begin{align*}
\beta_8\beta_8' &= -1 &
\beta_8 + \beta_8' &= 2 &
\beta_8^{k+2} &= 2\beta_8^{k+1} + \beta_8^k \\
\end{align*}

\subsection{Distances}
\begin{remark}
$\frac{1}{\beta_8} = \beta_8-2$
\end{remark}
\begin{table}[h!]
\begin{center}
\begin{tabular}{c|ccccc}
	\toprule
		\begin{tabular}[x]{@{}c@{}}Window\\size\end{tabular}&$\beta_8-2$	&	$\left( \beta_8-2, 3-\beta_8 \right)$	&	$3-\beta_8$	&	$\left( 3-\beta_8, 1 \right)$	&	$1$	\\
	\midrule
		&$3\beta_8+1$ 	&	$3\beta_8+1$	&				&				&				 	\\ 
		&$2\beta_8+1$	  &	$2\beta_8+1$	&	$2\beta_8+1$	&	$2\beta_8+1$	&				\\
		Distances&			&	            	&	            	&	$\beta_8+1$	&	$\beta_8+1$			\\
		&			          &	$\beta_8$		  &	$\beta_8$		  &	$\beta_8$		&	$\beta_8$		\\
	\bottomrule
\end{tabular}
%\caption{All possible distances between two consecutive points of the sequence of the quasicrystal with a window of the given size.}
\label{table:spaces}
\end{center}
\end{table}

\subsection{Distances}
Figure \ref{fig_8fold1DWindow} shows division of all windows by different Voronoi tiles and Figure \ref{fig_8fold1DTiles} shows corresponding Voronoi tiles for selected window sizes. The window sizes in the Figure \ref{fig_8fold1DTiles} correspond

\begin{figure}[h!]
\centering
\begin{tikzpicture}[scale=7]

\clip(-0.8,0) rectangle (0.8,-1.05);

\coordinate (O) at (0,0);
\draw [opacity=0.4] (-0.51500,-1.03000) -- (O) -- (0.51500,-1.03000);
\draw (-0.20711,-0.41422) -- (-0.50000,-1);
\draw (0.20711,-0.41422) -- (0.50000,-1);

\coordinate (trans01) at (0,-0.20711);
\draw [dotted]  ($(-0.20711,0)+2*(trans01)$) -- ($(0.55000,0)+2*(trans01)$);
%\draw [dotted]  ($( 0.03553,0)+2*(trans01)$) -- ($( 0.20711,0)+2*(trans01)$);
%\draw [dotted]  ($(-0.20711,0)+2*(trans01)$) -- ($(-0.03553,0)+2*(trans01)$);

\coordinate (trans02) at (0,-0.29289);
\draw [dotted]  ($(-0.29289,0)+2*(trans02)$) -- ($(0.55000,0)+2*(trans02)$);
\draw [dotted]  ($0.5*(-0.20711,0)+0.5*(-0.29289,0)+(trans01)+(trans02)$) -- ($(0.55000,0)+(trans01)+(trans02)$);
%\draw [|-|]  ( 0.12132,0) -- ( 0.29289,0);
%\draw [|-|]  (-0.29289,0) -- (-0.12132,0);

\coordinate (trans03) at (0,-0.41421);
\draw [dotted]  ($(-0.41421,0)+2*(trans03)$) -- ($(0.55000,0)+2*(trans03)$);
\draw [dotted]  ($0.5*(-0.29289,0)+0.5*(-0.41421,0)+(trans02)+(trans03)$) -- ($(0.55000,0)+(trans02)+(trans03)$);
%\draw [|-|]  (0,0) -- (0.17157,0);
%\draw [|-|]  (-0.17157,0) -- (0,0);

\coordinate (trans04) at (0,-0.50000);
\draw [dotted]  ($(-0.50000,0)+2*(trans04)$) -- ($(0.55000,0)+2*(trans04)$);
\draw [dotted]  ($0.5*(-0.41421,0)+0.5*(-0.50000,0)+(trans03)+(trans04)$) -- ($(0.55000,0)+(trans03)+(trans04)$);
%\draw [|-|]  (0.08579,0) -- (0.50000,0);
%\draw [|-|]  (-0.50000,0) -- (-0.08579,0);



\draw ($(-0.20711,0)+2*(trans01)$) -- ($(0.08579,0)+2*(trans04)$);
\draw ($(0.20711,0)+2*(trans01)$) -- ($(-0.08579,0)+2*(trans04)$);

\draw ($(-0.03553,0)+2*(trans01)$) -- ($(-0.12132,0)+2*(trans02)$);
\draw ($(0.03553,0)+2*(trans01)$) -- ($(0.12132,0)+2*(trans02)$);

\draw ($(-0.29289,0)+2*(trans02)$) -- ($(-0.08579,0)+2*(trans04)$);
\draw ($(0.29289,0)+2*(trans02)$) -- ($(0.08579,0)+2*(trans04)$);


\node [right] at ($(0.55000,0)+2*(trans01)$) {$\beta_8-2$};
\node [right] at ($(0.55000,0)+(trans01)+(trans02)$) {$\frac{1}{2}$};
\node [right] at ($(0.55000,0)+2*(trans02)$) {$3-\beta_8$};
\node [right] at ($(0.55000,0)+(trans02)+(trans03)$) {$\frac{\beta_8-1}{2}$};
\node [right] at ($(0.55000,0)+2*(trans03)$) {$2\beta_8-4$};
\node [right] at ($(0.55000,0)+(trans03)+(trans04)$) {$\frac{2\beta_8-3}{2}$};
\node [right] at ($(0.55000,0)+2*(trans04)$) {$1$};
\end{tikzpicture} 
\caption{Division of window into sections corresponding to different Voronoi tiles. Each vertical slice of the trapezoid represents a single window of size in $[\beta_8-2,1]$. The diagonal lines divide each window into the sections corresponding to different Voronoi tiles. }
\label{fig_8fold1DWindow}
\end{figure}


\begin{figure}[h!]
% a-2 -----------------------------------------------------------------------------
{$\beta_8-2$}:

\centering
\begin{tikzpicture}[scale=0.08]
\coordinate (O) at (0,0);
\coordinate (S) at (5.82843,0);
\coordinate (M) at (8.24264,0);
\coordinate (L) at (0,0);

\coordinate (space01) at (20,0);
\coordinate (space02) at (20,0);
%
%
\coordinate (left)  at (M);
\coordinate (right) at (S);

\coordinate (trans) at ($-1*(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (S);
\coordinate (right) at (S);

\coordinate (trans) at (0,0);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (S);
\coordinate (right) at (M);

\coordinate (trans) at ($(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
\end{tikzpicture}


\vspace{0.25cm}
% 1/2 -----------------------------------------------------------------------------
{$\frac{\beta_8-2+3-\beta_8}{2} = \frac{1}{2}$}:

\centering
\begin{tikzpicture}[scale=0.08]
\coordinate (O) at (0,0);
\coordinate (S) at (2.41421,0);
\coordinate (M) at (5.82843,0);
\coordinate (L) at (8.24264,0);

\coordinate (space01) at (20,0);
\coordinate (space02) at (40,0);
%
%
\coordinate (left)  at (S);
\coordinate (right) at (M);

\coordinate (trans) at ($-1*(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (L);
\coordinate (right) at (M);

\coordinate (trans) at ($-1*(space01)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (M);
\coordinate (right) at (M);

\coordinate (trans) at (0,0);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (M);
\coordinate (right) at (L);

\coordinate (trans) at ($(space01)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (M);
\coordinate (right) at (S);

\coordinate (trans) at ($(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
\end{tikzpicture}


\vspace{0.25cm}
% 3-a -----------------------------------------------------------------------------
{$3-\beta_8$}:

\centering
\begin{tikzpicture}[scale=0.08]
\coordinate (O) at (0,0);
\coordinate (S) at (2.41421,0);
\coordinate (M) at (5.82843,0);
\coordinate (L) at (0,0);

\coordinate (space01) at (20,0);
\coordinate (space02) at (40,0);
%
%
\coordinate (left)  at (S);
\coordinate (right) at (M);

\coordinate (trans) at ($-1*(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (M);
\coordinate (right) at (M);

\coordinate (trans) at (0,0);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (M);
\coordinate (right) at (S);

\coordinate (trans) at ($(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
\end{tikzpicture}

\vspace{0.25cm}
% (a-1)/2 -----------------------------------------------------------------------------
{$\frac{3-\beta_8+2\beta_8-4}{2} = \frac{\beta_8-1}{2}$}:

\centering
\begin{tikzpicture}[scale=0.08]
\coordinate (O) at (0,0);
\coordinate (S) at (2.41421,0);
\coordinate (M) at (3.41421,0);
\coordinate (L) at (5.82843,0);

\coordinate (space01) at (40,0);
\coordinate (space02) at (52,0);
%
%
\coordinate (left)  at (S);
\coordinate (right) at (M);

\coordinate (trans) at ($-1*(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (S);
\coordinate (right) at (L);

\coordinate (trans) at ($-1*(space01)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (L);
\coordinate (right) at (L);

\coordinate (trans) at (0,0);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (L);
\coordinate (right) at (S);

\coordinate (trans) at ($(space01)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (M);
\coordinate (right) at (S);

\coordinate (trans) at ($(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
\end{tikzpicture}



\vspace{0.25cm}
% 2a-4 -----------------------------------------------------------------------------
{$2\beta_8-4$}:

\centering
\begin{tikzpicture}[scale=0.08]
\coordinate (O) at (0,0);
\coordinate (S) at (2.41421,0);
\coordinate (M) at (3.41421,0);
\coordinate (L) at (5.82843,0);

\coordinate (space01) at (40,0);
\coordinate (space02) at (52,0);
%
%
\coordinate (left)  at (S);
\coordinate (right) at (M);

\coordinate (trans) at ($-1*(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (S);
\coordinate (right) at (L);

\coordinate (trans) at ($-1*(space01)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (L);
\coordinate (right) at (S);

\coordinate (trans) at ($(space01)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (M);
\coordinate (right) at (S);

\coordinate (trans) at ($(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
\end{tikzpicture}


\vspace{0.25cm}
% (2a-3)/2 -----------------------------------------------------------------------------
{$\frac{2\beta_8-4+1}{2} = \frac{2\beta_8-3}{2}$}:

\centering
\begin{tikzpicture}[scale=0.08]
\coordinate (O) at (0,0);
\coordinate (S) at (2.41421,0);
\coordinate (M) at (3.41421,0);
\coordinate (L) at (5.82843,0);

\coordinate (space01) at (40,0);
\coordinate (space02) at (52,0);
%
%
\coordinate (left)  at (S);
\coordinate (right) at (M);

\coordinate (trans) at ($-1*(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (S);
\coordinate (right) at (L);

\coordinate (trans) at ($-1*(space01)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (S);
\coordinate (right) at (S);

\coordinate (trans) at (0,0);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (L);
\coordinate (right) at (S);

\coordinate (trans) at ($(space01)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (M);
\coordinate (right) at (S);

\coordinate (trans) at ($(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
\end{tikzpicture}

\vspace{0.25cm}
% 1 -----------------------------------------------------------------------------
{$1$}:

\centering
\begin{tikzpicture}[scale=0.08]
\coordinate (O) at (0,0);
\coordinate (S) at (2.41421,0);
\coordinate (M) at (3.41421,0);
\coordinate (L) at (5.82843,0);

\coordinate (space01) at (40,0);
\coordinate (space02) at (52,0);
%
%
\coordinate (left)  at (S);
\coordinate (right) at (M);

\coordinate (trans) at ($-1*(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (S);
\coordinate (right) at (S);

\coordinate (trans) at (0,0);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
%
%
\coordinate (left)  at (M);
\coordinate (right) at (S);

\coordinate (trans) at ($(space02)$);
  \draw [dotted] ($-1*(left)+(trans)$) -- ($(right)+(trans)$);
  \draw [ultra thick]  ($-0.5*(left)+(trans)$) -- ($0.5*(right)+(trans)$);
  \fill ($(O)+(trans)$) circle[radius=0.6];
  \fill ($-1*(left)+(trans)$) circle[radius=0.6];
  \fill ($(right)+(trans)$) circle[radius=0.6];
\end{tikzpicture}
\caption{List of Voronoi tiles corresponding to sections of various windows. }
\label{fig_8fold1DTiles}
\end{figure}

\end{document}
